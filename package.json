{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "yarn workspaces run test",
        "build": "yarn workspaces run build",
        "infra": "yarn workspace infra",
        "ui": "yarn workspace frontend dev",
        "storybook": "yarn workspace frontend storybook"
    }
}
