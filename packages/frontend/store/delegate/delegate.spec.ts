import delegate from '@/store/delegate'

describe('delegate store module', () => {
  it('fails until a passing test is added', () => {
    expect(true).toBe(true)
    // TODO: update once the store does something other than call API
  })
})
