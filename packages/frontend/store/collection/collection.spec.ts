import collection from '@/store/collection'

describe('collection store module', () => {
  it('fails until a passing test is added', () => {
    expect(true).toBe(true)
    // TODO: update once the store does something
  })
})
